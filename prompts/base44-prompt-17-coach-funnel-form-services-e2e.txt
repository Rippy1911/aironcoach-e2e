Airon Coach — one UserProfile row, coach fields on same row.

## 17 — Coach form + Services E2E

**Bugs:**
- CreateCoachProfile: specialty chips lost on 2nd save ("Select at least one specialty")
- Form still narrow (`max-w-sm`); no desktop preview
- Services: archive in bundle but create→archive→restore not verified E2E
- Settings Coach panel vs `/CreateCoachProfile` show inconsistent data

**Fix:**
- Chips bound to form state — survive validation/refetch/reload
- Desktop: 2-col form + live preview card, `max-w-5xl/6xl`
- Services: full flow create offer → list → archive → archived section → restore (use `manageCoachServiceTier`)
- Settings Coach: same data as wizard + "Edit full coach profile" link

**Done when:** `coach-create-probe.mjs` passes; one test offer archived and restored.
