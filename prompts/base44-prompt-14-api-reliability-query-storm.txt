Airon Coach — unified account, coach = capability on UserProfile, Trainee not Athlete. No mode switcher.

## 14 — API / query storm

**Prod bugs:** burst `UserProfile` queries → 429; `classifyConversationTier` 404; `getWorkoutsWithSets` 500 on Home.

**Fix:**
- One cached canonical profile fetch per session (`created_by_id` + existing `yse`/`uu`). Kill parallel `created_by` email polls on same page load.
- 429: dedupe in-flight, backoff retry, user-facing Retry — not infinite spinners.
- Deploy `classifyConversationTier` OR silence caller. Fix `getWorkoutsWithSets` to return 200 + empty array when no data.

**Done when:** 5-route nav in 30s → ≤2 UserProfile calls, no 429, Home no 500. Coach funnel still works.
