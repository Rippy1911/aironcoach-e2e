Airon Coach — deactivate/delete coach ≠ delete account.

## 19 — Lifecycle delete + Settings coach UX

**Gap:** pause/deactivate/reactivate ✅ in bundle; **Delete public coach data** missing.

**Fix:**
- Settings → Coach Profile: one panel — visibility, pause, deactivate, reactivate, danger zone
- Delete public coach data: typed `DELETE` confirm → clear coach public fields, archive tiers, `is_coach=false`; keep account/workouts/messages/billing
- Deactivated coach with saved data → Hub shows Reactivate, not "create profile first"
- Nav: paused=hidden from directory; deactivated=hide Hub or show Reactivate CTA

**Done when:** delete flow works on disposable test coach; Settings coach panel unified.
