Airon Coach — unified account, Trainee not Athlete.

## 15 — Routing

**Bugs:**
- `/Settings?tab=coach` → loads Account, not Coach panel (click sub-nav works)
- `/MyFollows` → empty/loading; menu Follows → same orphan
- `/Messages`, `/Teams` → sometimes login wall while rest of app is authed

**Fix:**
- Settings: sync `?tab=` and `#hash` on load + sub-nav click + back/forward
- `/MyFollows` → redirect `/UserProfile?id=<self>&tab=follows`; update menu link
- Messages/Teams: same auth bootstrap as Home — spinner while loading, no false redirect

**Done when:** cold-load URLs above work logged in.
