Airon Coach — public coach profile = business card.

## 18 — Public coach default tab + card

**Bug:** Directory / `/UserProfile?id=<coach>` opens Overview; Coach tab last or inactive. Coach tab content narrow (~50%).

**Fix:**
- Public coach (`is_coach` + business name): default tab = `coach` unless `?tab=` explicit
- Directory links + Copy profile link → `&tab=coach`
- Coach tab: 2-col card `max-w-6xl` — hero, bio, specialties, offers, CTAs (Follow/Message); self gets Edit/Manage
- Non-coach profiles: default `overview` unchanged

**Done when:** Coach Directory click lands on Coach tab; card uses width properly.
