node_modules/
playwright-report/
test-results/
blob-report/
playwright/.cache/

.env
.env.local
.env.*.local

e2e/.auth/*.json

# Promo capture output is regenerated per run; commit ad-hoc, not by default.
promo-assets/

# One-off verification screenshots/reports (contain test-account UI captures)
verify-out/

*.log
.DS_Store

# Cursor MCP config (per-repo runtime config -- copy from .example)
.cursor/mcp.json
